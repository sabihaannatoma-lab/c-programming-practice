#include <stdio.h>

int main() {
    int i, N, S = 0;
    printf("Enter the last number of series: ");
    scanf("%d", &N);

    for(i = 1; i <= N; i++) {
        S = S + i;
    }

    printf("Sum of the series = %d\n", S);
    return 0;
}
